.row
  %p.pull-right
    = t(".count_info", :count => (render_cell :users, :count)) 

%section#activity_users
  .page-header
    %h4
      = t(".activity_users")
  = render_cell :users, :activity

%br
%br

%section#recent_join_users
  .page-header
    %h4
      = t(".recent_join_users")
  = render_cell :users, :recent_join