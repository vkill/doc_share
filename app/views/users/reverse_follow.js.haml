- if @follow
  = raw %Q`$("[data-user-reverse-follow-#{@target_user.id}]").html(I18n.t("web.page.user.unfollow"))`
- else
  = raw %Q`$("[data-user-reverse-follow-#{@target_user.id}]").html(I18n.t("web.page.user.follow"))`

= raw %Q`$("[data-user-followers-count-#{@target_user.id}]").html("#{@target_user.followers_count}")`

