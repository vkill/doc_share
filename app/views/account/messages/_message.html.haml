.page-header
  %p.pull-right
    - if @can_reply
      = link_to t(:reply, :scope => [:web, :page, :message]), [:account, message]
    = link_to t(:delete), [:account, message], :method => :delete, :confirm => t(:delete_confirm)

  %p
    = message.opened_text
    = link_to message.topic, [:account, message]
  %p
    = message.body