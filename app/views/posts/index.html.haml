%section#posts
  .row
    .span10
      - if @posts.present?
        = render @posts
      - else
        = collection_empty(:posts)
      
      = render 'partials/pagination', :resources => @posts
    
    = render "right_bar"
