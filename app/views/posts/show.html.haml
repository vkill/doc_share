%section#post
  .row
    .span10
      = render @post
      
      .page-header
      %br
      %br
      .row#comments
        .span10
          %h4= @post.comments_count.to_s << " comments"
          = render @comments
      %br
      = render "comments/form", :comment => @comment

    = render "right_bar"