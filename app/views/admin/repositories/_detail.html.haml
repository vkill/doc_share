
= show_for resource do |repository|
  = repository.attributes :id
  = repository.attribute :user_id do
    = link_to resource.username, [:admin, resource.user]
  = repository.attribute :category_id do
    = link_to resource.category_name, [:admin, resource.category]
  = repository.attributes :name, :describtion

  = repository.attribute :created_at do
    = l resource.created_at
  = repository.attribute :updated_at do
    = l resource.updated_at
  
  = repository.association :repo_files do |repo_file|
    = link_to repo_file.repo_file, [:download_repo_file, :admin, resource, repo_file]
    = raw " <br/> "
    